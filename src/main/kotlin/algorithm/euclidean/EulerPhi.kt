package algorithm.euclidean

/**
 * Euler's totient function. [invoke] uses a sieve internally, [phiByFactorization]
 * computes the same value directly from n's prime factorization.
 */
class EulerPhi {
    operator fun invoke(n: Int): Int {
        TODO("Implement Euler's totient via sieve")
    }

    fun phiByFactorization(n: Int): Int {
        TODO("Implement Euler's totient via prime factorization")
    }
}
