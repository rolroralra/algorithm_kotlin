package algorithm.segmenttree.binaryindexedtree

/**
 * 0-indexed Binary Indexed Tree (Fenwick tree variant) over a fixed [capacity] for range-sum queries.
 */
class BinaryIndexTree(private val capacity: Int) {
    fun update(index: Int, value: Int) {
        TODO("Implement point update")
    }

    fun query(leftIndexInclusive: Int, rightIndexInclusive: Int): Int {
        TODO("Implement range-sum query")
    }
}
